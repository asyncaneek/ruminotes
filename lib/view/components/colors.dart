// color definitions per component of the app

import 'package:flutter/material.dart';

const Color background_color_app = Color(0xFF343434);
const Color background_color_card = Color(0xFF757575);

const BoxDecoration card_decoration = BoxDecoration(
    borderRadius: BorderRadius.all(Radius.circular(12.5)),
    color: background_color_card);
